package main

//	"encoding/json"

type Data map[string]interface{}

/*
func parseJSON(JSON []byte) struct {
	var i interface{}
	json.Unmarshal(JSON,i)
	if m,ok:= i.(map[string]interface{});ok{

	}
}
*/
